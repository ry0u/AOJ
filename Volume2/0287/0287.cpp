#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <sstream>

#define REP(i,k,n) for(int i=k;i<n;i++)
#define rep(i,n) for(int i=0;i<n;i++)

using namespace std;

struct BigInt
{
	int block[100];

};

int main()
{
	int m,n;
	while(cin >> m >>  n)
	{
		if(m == 0 && n == 0) break;


	}

	return 0;
}
